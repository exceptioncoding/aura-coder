run:
  timeout: 10m

linters:
  enable:
    - errcheck
    - gofmt
    - goimports
    - gosec
    - staticcheck
    - unused
    - revive
    - gocritic
    - misspell

issues:
  max-issues-per-linter: 0
  max-same-issues: 0
